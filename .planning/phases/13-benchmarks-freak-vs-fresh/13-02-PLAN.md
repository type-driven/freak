---
phase: 13-benchmarks-freak-vs-fresh
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - packages/benchmarks/scripts/check_tools.ts
  - packages/benchmarks/scripts/bench_throughput.ts
  - packages/benchmarks/scripts/bench_build.ts
  - packages/benchmarks/scripts/bench_bundle.ts
  - packages/benchmarks/scripts/bench_startup.ts
  - packages/benchmarks/scripts/bench.ts
  - packages/benchmarks/RESULTS.md
autonomous: true

must_haves:
  truths:
    - "Running deno task bench in packages/benchmarks produces RESULTS.md with real numbers"
    - "RESULTS.md contains a summary table with all four dimensions for all three apps"
    - "Effect overhead percentage is calculated and documented"
    - "Methodology section explains tools, settings, and environment"
    - "check_tools.ts exits with clear error if oha or hyperfine is not installed"
  artifacts:
    - path: "packages/benchmarks/scripts/check_tools.ts"
      provides: "Tool availability checker"
      contains: "oha"
    - path: "packages/benchmarks/scripts/bench_throughput.ts"
      provides: "HTTP throughput benchmark via oha"
      contains: "oha"
    - path: "packages/benchmarks/scripts/bench_build.ts"
      provides: "Build time benchmark via hyperfine"
      contains: "hyperfine"
    - path: "packages/benchmarks/scripts/bench_bundle.ts"
      provides: "Bundle size calculator"
      contains: "_fresh/static"
    - path: "packages/benchmarks/scripts/bench_startup.ts"
      provides: "Startup time measurement"
      contains: "fetch"
    - path: "packages/benchmarks/scripts/bench.ts"
      provides: "Main orchestrator that runs all dimensions"
      contains: "RESULTS.md"
    - path: "packages/benchmarks/RESULTS.md"
      provides: "Generated benchmark results report"
      contains: "Summary Table"
  key_links:
    - from: "packages/benchmarks/scripts/bench.ts"
      to: "packages/benchmarks/scripts/bench_throughput.ts"
      via: "import"
      pattern: "import.*bench_throughput"
    - from: "packages/benchmarks/scripts/bench.ts"
      to: "packages/benchmarks/RESULTS.md"
      via: "Deno.writeTextFile"
      pattern: "RESULTS.md"
    - from: "packages/benchmarks/scripts/bench_throughput.ts"
      to: "packages/benchmarks/apps/freak-app/_fresh/server.js"
      via: "Deno.Command spawn deno serve"
      pattern: "server.js"
---

<objective>
Create the benchmark measurement scripts and orchestrator, then run the full benchmark
suite to produce RESULTS.md with real comparative numbers.

Purpose: This is the deliverable of Phase 13 — a published, reproducible benchmark
report documenting Freak's performance relative to upstream Fresh 2.

Output: Six scripts in packages/benchmarks/scripts/ plus a generated RESULTS.md with
methodology, raw data, and summary table.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-benchmarks-freak-vs-fresh/13-RESEARCH.md
@.planning/phases/13-benchmarks-freak-vs-fresh/13-01-SUMMARY.md

# Scripts reference the three apps created in Plan 01
@packages/benchmarks/deno.json
@packages/benchmarks/apps/freak-app/deno.json
@packages/benchmarks/apps/freak-plain-app/deno.json
@packages/benchmarks/apps/upstream-app/deno.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create benchmark scripts</name>
  <files>
    packages/benchmarks/scripts/check_tools.ts
    packages/benchmarks/scripts/bench_throughput.ts
    packages/benchmarks/scripts/bench_build.ts
    packages/benchmarks/scripts/bench_bundle.ts
    packages/benchmarks/scripts/bench_startup.ts
    packages/benchmarks/scripts/bench.ts
  </files>
  <action>
    Create six Deno scripts in `packages/benchmarks/scripts/`. All scripts use `Deno.Command` for subprocess management. All paths should be resolved relative to the scripts directory using `import.meta.dirname` or `new URL("../", import.meta.url)`.

    Define a shared constant for the three apps used across all scripts:

    ```typescript
    const APPS = [
      { name: "upstream", dir: "apps/upstream-app", port: 8003 },
      { name: "freak-plain", dir: "apps/freak-plain-app", port: 8002 },
      { name: "freak-effect", dir: "apps/freak-app", port: 8001 },
    ] as const;
    ```

    **scripts/check_tools.ts:**
    - Export `async function checkTools(): Promise<void>`
    - Check for `oha` and `hyperfine` using `Deno.Command("which", { args: [name] })` with `stdout: "null"`, `stderr: "null"` — check exit code 0
    - If either is missing, print clear install instructions and throw:
      ```
      Missing required tools:
        - oha: brew install oha
        - hyperfine: brew install hyperfine
      ```
    - Export the function so bench.ts can call it

    **scripts/bench_throughput.ts:**
    - Export `async function runThroughputBench(benchRoot: string): Promise<ThroughputResult[]>`
    - Define `ThroughputResult` type: `{ name: string, requestsPerSec: number, latencyP50: number, latencyP90: number, latencyP99: number }`
    - For each app in APPS:
      1. Build the app first: `deno run -A {appDir}/dev.ts build` (cwd: benchRoot)
      2. Start server: `deno serve -A --port={port} _fresh/server.js` (cwd: resolved appDir)
      3. Poll `http://localhost:{port}/` until ready (max 50 attempts, 100ms apart)
      4. Run warmup: `oha -n 2000 -c 10 --no-tui http://localhost:{port}/api/todos` (discard output)
      5. Run measurement: `oha -n 10000 -c 50 --no-tui --output-format json http://localhost:{port}/api/todos`
      6. Parse JSON stdout — extract `summary.requestsPerSec`, `latencyPercentiles.p50`, `.p90`, `.p99` (all in seconds, convert latency to milliseconds for display)
      7. Kill server with SIGTERM, await status
      8. Push result to array
    - Return results array
    - IMPORTANT: Kill any existing processes on the port before starting: `lsof -ti:{port} | xargs kill -9 2>/dev/null; true`

    **scripts/bench_build.ts:**
    - Export `async function runBuildTimeBench(benchRoot: string): Promise<BuildResult[]>`
    - Define `BuildResult` type: `{ name: string, meanSecs: number, stddevSecs: number, runs: number }`
    - Use `hyperfine` to compare all three apps in a single invocation:
      ```
      hyperfine --warmup 1 --runs 5 --export-json /tmp/bench_build.json \
        --prepare 'rm -rf apps/freak-app/_fresh' \
        'deno run -A apps/freak-app/dev.ts build' \
        --prepare 'rm -rf apps/freak-plain-app/_fresh' \
        'deno run -A apps/freak-plain-app/dev.ts build' \
        --prepare 'rm -rf apps/upstream-app/_fresh' \
        'deno run -A apps/upstream-app/dev.ts build'
      ```
      Run with cwd set to benchRoot.
    - Actually, hyperfine with multiple commands and per-command `--prepare` is tricky. Simpler approach: run hyperfine THREE times, once per app, each with its own --prepare and --export-json to a temp file. Parse each temp file.
    - Parse hyperfine JSON: `results[0].mean` (seconds), `results[0].stddev` (seconds)
    - Return array of BuildResult

    **scripts/bench_bundle.ts:**
    - Export `async function runBundleSizeBench(benchRoot: string): Promise<BundleResult[]>`
    - Define `BundleResult` type: `{ name: string, totalRawKB: number, totalGzipKB: number, fileCount: number }`
    - For each app:
      1. Build the app: `deno run -A {appDir}/dev.ts build` (cwd: benchRoot)
      2. Walk `{appDir}/_fresh/static/` recursively
      3. For each `.js` file: read file, sum `file.byteLength` for raw size
      4. For gzip size: use `new CompressionStream("gzip")` on each file's bytes, sum compressed sizes
      5. Report total raw KB, total gzip KB, file count
    - Return array of BundleResult

    **scripts/bench_startup.ts:**
    - Export `async function runStartupBench(benchRoot: string): Promise<StartupResult[]>`
    - Define `StartupResult` type: `{ name: string, meanMs: number, runs: number }`
    - For each app, run 5 iterations:
      1. Build app once before the loop (deno run -A dev.ts build)
      2. Per iteration:
         a. Record `performance.now()` as start
         b. Spawn `deno serve -A --port={port} _fresh/server.js` (cwd: appDir)
         c. Poll `http://localhost:{port}/` every 50ms until 200 response (max 100 attempts = 5s timeout)
         d. Record `performance.now()` as end
         e. Kill server, await status
         f. Store `end - start` in milliseconds
      3. After loop: compute mean of 5 runs
    - Return array of StartupResult
    - Kill any existing process on the port before each iteration

    **scripts/bench.ts:**
    - This is the main orchestrator. Export nothing — runs as a script.
    - Import and call all other scripts:
      ```typescript
      import { checkTools } from "./check_tools.ts";
      import { runThroughputBench } from "./bench_throughput.ts";
      import { runBuildTimeBench } from "./bench_build.ts";
      import { runBundleSizeBench } from "./bench_bundle.ts";
      import { runStartupBench } from "./bench_startup.ts";
      ```
    - Resolve benchRoot: `new URL("../", import.meta.url).pathname`
    - Step 1: `await checkTools()`
    - Step 2: Print "Running throughput benchmark..." then `const throughput = await runThroughputBench(benchRoot)`
    - Step 3: Print "Running build time benchmark..." then `const buildTime = await runBuildTimeBench(benchRoot)`
    - Step 4: Print "Running bundle size benchmark..." then `const bundleSize = await runBundleSizeBench(benchRoot)`
    - Step 5: Print "Running startup time benchmark..." then `const startup = await runStartupBench(benchRoot)`
    - Step 6: Collect system metadata:
      - `Deno.build.arch`, `Deno.build.os`
      - `Deno.version.deno`, `Deno.version.v8`, `Deno.version.typescript`
      - Date: `new Date().toISOString().split("T")[0]`
    - Step 7: Render RESULTS.md using template (see below). Helper function `renderResults(meta, throughput, buildTime, bundleSize, startup): string` that builds the markdown string.
    - Step 8: Write to `{benchRoot}/RESULTS.md`
    - Step 9: Print "Benchmark complete! Results written to packages/benchmarks/RESULTS.md"

    **RESULTS.md template rendered by bench.ts:**
    ```markdown
    # Freak vs Fresh 2 Benchmark Results

    **Date:** {date}
    **Machine:** {os} {arch}
    **Deno:** {denoVersion} (V8 {v8Version})
    **Freak version:** 2.2.1 (local)
    **Upstream Fresh:** jsr:@fresh/core@2.2.0
    **Effect:** npm:effect@4.0.0-beta.20

    ## Summary Table

    | Dimension | Upstream Fresh | Freak (plain) | Freak (Effect) | Effect overhead |
    |-----------|---------------|---------------|----------------|-----------------|
    | Throughput (req/s) | {upstream} | {plain} | {effect} | {pct}% |
    | Build time (s) | {upstream} | {plain} | {effect} | {pct}% |
    | Bundle size (KB gzip) | {upstream} | {plain} | {effect} | +{delta} KB |
    | Startup time (ms) | {upstream} | {plain} | {effect} | +{delta} ms |

    ## Methodology

    - **Throughput:** `oha -n 10000 -c 50` against `GET /api/todos` (in-memory JSON response, no I/O)
    - **Build time:** `hyperfine --warmup 1 --runs 5` with `--prepare 'rm -rf _fresh'` (cold AOT build)
    - **Bundle size:** Sum of all `.js` files in `_fresh/static/` after build (raw + gzip via CompressionStream)
    - **Startup time:** 5 iterations of spawn server -> poll until 200 -> measure elapsed time
    - **Environment:** Pre-warmed Deno module cache; no network downloads during measurement

    ## Raw Results

    ### Handler Throughput
    | App | req/s | p50 (ms) | p90 (ms) | p99 (ms) |
    |-----|-------|----------|----------|----------|
    {throughput rows}

    ### Build Time
    | App | Mean (s) | Stddev (s) | Runs |
    |-----|----------|------------|------|
    {buildTime rows}

    ### Bundle Size
    | App | Raw (KB) | Gzip (KB) | Files |
    |-----|----------|-----------|-------|
    {bundleSize rows}

    ### Startup Time
    | App | Mean (ms) | Runs |
    |-----|-----------|------|
    {startup rows}

    ## Notes on Effect Overhead

    {Auto-generated commentary based on the numbers:}
    - If Effect throughput is within 5% of plain: "Effect handler overhead is negligible for this workload."
    - If Effect throughput is 5-20% slower: "Effect adds measurable overhead (~X%) due to fiber scheduling and layer resolution. This is the cost of the ManagedRuntime dispatch path."
    - If Effect bundle is larger: "Effect adds X KB (gzip) to the client bundle due to the effect npm dependency being included in island imports (if any). For server-only Effect usage, there is no bundle size impact."
    - If build time differs: note whether the difference is from additional esbuild entries or just noise.
    ```

    Compute "Effect overhead" as:
    - Throughput: `((plain - effect) / plain * 100).toFixed(1)` — negative means Effect is slower
    - Build time: `((effect - upstream) / upstream * 100).toFixed(1)` — positive means slower
    - Bundle size: `(effectGzip - upstreamGzip).toFixed(1)` — absolute KB difference
    - Startup: `(effectMs - upstreamMs).toFixed(0)` — absolute ms difference

    For the "Effect overhead" column in the summary table, compare Freak (Effect) against Freak (plain) for throughput (isolates Effect cost from Freak fork cost), and compare Freak (Effect) against Upstream for build/bundle/startup (shows total overhead).
  </action>
  <verify>
    1. `deno check packages/benchmarks/scripts/bench.ts` — type-checks without error
    2. `deno check packages/benchmarks/scripts/check_tools.ts` — type-checks without error
    3. `deno check packages/benchmarks/scripts/bench_throughput.ts` — type-checks without error
    4. `deno check packages/benchmarks/scripts/bench_build.ts` — type-checks without error
    5. `deno check packages/benchmarks/scripts/bench_bundle.ts` — type-checks without error
    6. `deno check packages/benchmarks/scripts/bench_startup.ts` — type-checks without error
  </verify>
  <done>
    All six benchmark scripts exist, type-check, and the orchestrator (bench.ts) imports from all dimension scripts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the benchmark suite and commit RESULTS.md</name>
  <files>
    packages/benchmarks/RESULTS.md
  </files>
  <action>
    1. Verify tools are installed: `which oha && which hyperfine` — if either is missing, run `brew install oha hyperfine` and retry.

    2. Kill any processes on ports 8001-8003: `lsof -ti:8001,8002,8003 | xargs kill -9 2>/dev/null; true`

    3. Warm the module cache for all three apps:
       - `deno cache packages/benchmarks/apps/freak-app/main.ts`
       - `deno cache packages/benchmarks/apps/freak-plain-app/main.ts`
       - `deno cache packages/benchmarks/apps/upstream-app/main.ts`

    4. Run the full benchmark: `deno run -A packages/benchmarks/scripts/bench.ts`
       - This will take several minutes (throughput: ~3 min, build: ~2 min, bundle: ~30s, startup: ~1 min)
       - Total expected time: 5-8 minutes

    5. Verify RESULTS.md was generated: `cat packages/benchmarks/RESULTS.md`
       - Must contain the Summary Table with real numbers (not placeholders)
       - Must contain all four dimension sections with data

    6. If any benchmark dimension fails (e.g., oha returns error, server doesn't start):
       - Debug the specific script
       - Check if the app builds and starts correctly: `deno run -A apps/freak-app/dev.ts build && deno serve -A --port=8001 apps/freak-app/_fresh/server.js`
       - Fix and re-run

    7. Review the generated RESULTS.md for obvious errors:
       - All numbers should be non-zero
       - Throughput should be in the thousands of req/s range
       - Build time should be in the 1-30 second range
       - Bundle size should be in the 10-500 KB range
       - Startup time should be in the 100-5000 ms range
  </action>
  <verify>
    1. `test -f packages/benchmarks/RESULTS.md` — file exists
    2. `grep "Summary Table" packages/benchmarks/RESULTS.md` — contains summary
    3. `grep "req/s" packages/benchmarks/RESULTS.md` — contains throughput data
    4. `grep "Build Time" packages/benchmarks/RESULTS.md` — contains build data
    5. `grep "Bundle Size" packages/benchmarks/RESULTS.md` — contains bundle data
    6. `grep "Startup Time" packages/benchmarks/RESULTS.md` — contains startup data
    7. `grep "Effect overhead" packages/benchmarks/RESULTS.md` — contains analysis
  </verify>
  <done>
    RESULTS.md exists at packages/benchmarks/RESULTS.md with real benchmark numbers for all four dimensions across all three apps. The summary table shows Effect overhead percentages. The methodology section documents the measurement approach.
  </done>
</task>

</tasks>

<verification>
- `deno run -A packages/benchmarks/scripts/check_tools.ts` exits 0 (tools available)
- `deno check packages/benchmarks/scripts/bench.ts` exits 0 (type-checks)
- `packages/benchmarks/RESULTS.md` exists with non-placeholder data
- Summary table has entries for all three apps across all four dimensions
- Effect overhead is calculated and documented
- Methodology section explains measurement approach
</verification>

<success_criteria>
1. Running `deno task --cwd=packages/benchmarks bench` produces RESULTS.md with real numbers
2. RESULTS.md summary table has all four dimensions filled in for all three apps
3. Effect overhead is quantified (percentage for throughput/build, absolute for bundle/startup)
4. All six scripts type-check with `deno check`
5. RESULTS.md is committed to the repository
</success_criteria>

<output>
After completion, create `.planning/phases/13-benchmarks-freak-vs-fresh/13-02-SUMMARY.md`
</output>
