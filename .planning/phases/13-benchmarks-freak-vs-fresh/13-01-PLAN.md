---
phase: 13-benchmarks-freak-vs-fresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/benchmarks/deno.json
  - packages/benchmarks/apps/freak-app/deno.json
  - packages/benchmarks/apps/freak-app/main.ts
  - packages/benchmarks/apps/freak-app/dev.ts
  - packages/benchmarks/apps/freak-app/routes/index.tsx
  - packages/benchmarks/apps/freak-app/routes/api/todos.ts
  - packages/benchmarks/apps/freak-app/islands/Counter.tsx
  - packages/benchmarks/apps/freak-app/services/TodoService.ts
  - packages/benchmarks/apps/freak-plain-app/deno.json
  - packages/benchmarks/apps/freak-plain-app/main.ts
  - packages/benchmarks/apps/freak-plain-app/dev.ts
  - packages/benchmarks/apps/freak-plain-app/routes/index.tsx
  - packages/benchmarks/apps/freak-plain-app/routes/api/todos.ts
  - packages/benchmarks/apps/freak-plain-app/islands/Counter.tsx
  - packages/benchmarks/apps/upstream-app/deno.json
  - packages/benchmarks/apps/upstream-app/main.ts
  - packages/benchmarks/apps/upstream-app/dev.ts
  - packages/benchmarks/apps/upstream-app/routes/index.tsx
  - packages/benchmarks/apps/upstream-app/routes/api/todos.ts
  - packages/benchmarks/apps/upstream-app/islands/Counter.tsx
  - packages/benchmarks/.gitignore
  - deno.json
autonomous: true

must_haves:
  truths:
    - "Three benchmark apps exist with identical route structure and feature parity"
    - "freak-app uses createEffectApp with Effect handler for /api/todos"
    - "freak-plain-app uses plain Freak handlers (no Effect) for /api/todos"
    - "upstream-app uses jsr:@fresh/core@2.2.0 for /api/todos"
    - "All three apps build successfully with deno task build"
    - "packages/benchmarks is a workspace member in root deno.json"
  artifacts:
    - path: "packages/benchmarks/deno.json"
      provides: "Workspace package config with bench tasks"
      contains: "@fresh/benchmarks"
    - path: "packages/benchmarks/apps/freak-app/main.ts"
      provides: "Freak Effect app entry point"
      contains: "createEffectApp"
    - path: "packages/benchmarks/apps/freak-plain-app/main.ts"
      provides: "Freak plain app entry point"
      contains: "App"
    - path: "packages/benchmarks/apps/upstream-app/main.ts"
      provides: "Upstream Fresh app entry point"
      contains: "App"
    - path: "packages/benchmarks/apps/freak-app/routes/api/todos.ts"
      provides: "Effect handler returning JSON todos"
      contains: "Effect.gen"
    - path: "packages/benchmarks/apps/freak-plain-app/routes/api/todos.ts"
      provides: "Plain handler returning JSON todos"
    - path: "packages/benchmarks/apps/upstream-app/routes/api/todos.ts"
      provides: "Upstream handler returning JSON todos"
  key_links:
    - from: "packages/benchmarks/apps/freak-app/deno.json"
      to: "packages/fresh/src/mod.ts"
      via: "import map @fresh/core"
      pattern: '"@fresh/core".*../../fresh/src/mod.ts'
    - from: "packages/benchmarks/apps/upstream-app/deno.json"
      to: "jsr:@fresh/core@2.2.0"
      via: "import map @fresh/core"
      pattern: '"@fresh/core".*jsr:@fresh/core@2.2.0'
    - from: "deno.json"
      to: "packages/benchmarks"
      via: "workspace member"
      pattern: "packages/benchmarks"
---

<objective>
Create the `packages/benchmarks/` directory with three minimal Fresh apps that have
identical route structure (GET / SSR page, GET /api/todos JSON endpoint, one Counter
island) so they can be compared apples-to-apples in Plan 02.

Purpose: Establish the benchmark scaffold that the measurement scripts will target.
Three apps isolate three variables: upstream Fresh baseline, Freak without Effect, and
Freak with Effect.

Output: Three buildable Fresh apps, a workspace deno.json, and a .gitignore for build
artifacts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-benchmarks-freak-vs-fresh/13-RESEARCH.md

# Reference patterns from the existing example app
@packages/examples/effect-integration/main.ts
@packages/examples/effect-integration/dev.ts
@packages/examples/effect-integration/deno.json
@packages/examples/effect-integration/routes/index.tsx
@packages/examples/effect-integration/routes/api/todos.ts
@packages/examples/effect-integration/services/TodoService.ts
@packages/examples/effect-integration/services/layers.ts

# Framework entry points
@packages/fresh/src/mod.ts
@packages/fresh/deno.json
@packages/effect/deno.json
@packages/effect/src/mod.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three benchmark apps with parity routes</name>
  <files>
    packages/benchmarks/deno.json
    packages/benchmarks/.gitignore
    packages/benchmarks/apps/freak-app/deno.json
    packages/benchmarks/apps/freak-app/main.ts
    packages/benchmarks/apps/freak-app/dev.ts
    packages/benchmarks/apps/freak-app/routes/index.tsx
    packages/benchmarks/apps/freak-app/routes/api/todos.ts
    packages/benchmarks/apps/freak-app/islands/Counter.tsx
    packages/benchmarks/apps/freak-app/services/TodoService.ts
    packages/benchmarks/apps/freak-plain-app/deno.json
    packages/benchmarks/apps/freak-plain-app/main.ts
    packages/benchmarks/apps/freak-plain-app/dev.ts
    packages/benchmarks/apps/freak-plain-app/routes/index.tsx
    packages/benchmarks/apps/freak-plain-app/routes/api/todos.ts
    packages/benchmarks/apps/freak-plain-app/islands/Counter.tsx
    packages/benchmarks/apps/upstream-app/deno.json
    packages/benchmarks/apps/upstream-app/main.ts
    packages/benchmarks/apps/upstream-app/dev.ts
    packages/benchmarks/apps/upstream-app/routes/index.tsx
    packages/benchmarks/apps/upstream-app/routes/api/todos.ts
    packages/benchmarks/apps/upstream-app/islands/Counter.tsx
  </files>
  <action>
    Create the benchmark scaffold:

    **packages/benchmarks/deno.json:**
    - name: `@fresh/benchmarks`, version: `0.1.0`
    - Tasks: `bench` (runs scripts/bench.ts), `bench:throughput`, `bench:build`, `bench:bundle`, `bench:startup`, `build:all` (builds all 3 apps sequentially), `build:freak` (deno run -A apps/freak-app/dev.ts build), `build:freak-plain` (deno run -A apps/freak-plain-app/dev.ts build), `build:upstream` (deno run -A apps/upstream-app/dev.ts build)
    - No compilerOptions here — each app has its own deno.json with JSX config

    **packages/benchmarks/.gitignore:**
    - `apps/*/_fresh/` — build artifacts from all apps
    - `*.json.tmp` — temp benchmark output files

    **freak-app (Effect handler, port 8001):**
    - `deno.json`: imports `@fresh/core` from `../../../fresh/src/mod.ts`, `@fresh/core/dev` from `../../../fresh/src/dev/mod.ts`, `@fresh/core/runtime` from `../../../fresh/src/runtime/shared.ts`, `@fresh/effect` from `../../../effect/src/mod.ts`, `effect` from `npm:effect@^4.0.0-beta.20`, `preact` from `npm:preact@^10.28.3`, `preact/hooks` from `npm:preact@^10.28.3/hooks`. Include compilerOptions with `jsx: precompile`, `jsxImportSource: preact`, same jsxPrecompileSkipElements as the example app. Tasks: `dev` (deno run -A --watch dev.ts), `build` (deno run -A dev.ts build), `start` (deno serve -A _fresh/server.js)
    - `services/TodoService.ts`: Minimal Effect service — `TodoService` via `ServiceMap.Service` with a single `list()` method returning `Effect.Effect<Array<{id: string, text: string, done: boolean}>>`. Implementation: `Layer.succeed(TodoService, { list: () => Effect.sync(() => [{id: "1", text: "Benchmark todo", done: false}]) })`. Export `TodoLayer` and `TodoServiceR` type. Import Effect, Layer, ServiceMap from "effect".
    - `main.ts`: `import { createEffectApp } from "@fresh/effect"`, `import { staticFiles } from "@fresh/core"`, create `const effectApp = createEffectApp({ layer: TodoLayer })`. Export pattern: `export const app = effectApp.use(staticFiles()).fsRoutes()`. Builder.listen() auto-unwraps EffectApp instances.
    - `dev.ts`: Same as the example app — `import { Builder } from "@fresh/core/dev"`, create builder with `target: "safari12"`, no tailwind, if args includes "build" call `builder.build()` else `builder.listen(() => import("./main.ts"))`.
    - `routes/index.tsx`: Simple SSR page rendering `<h1>Freak Benchmark (Effect)</h1>` with a `<Counter />` island. Import `Counter` from `../islands/Counter.tsx`. No Effect usage on this route (it's for bundle size parity). Export default function component.
    - `routes/api/todos.ts`: Effect handler using `Effect.gen` — yields `TodoService`, calls `svc.list()`, returns `new Response(JSON.stringify(todos), { headers: {"content-type": "application/json"} })`. Use simple pattern: export `handler` as `{ GET: (ctx) => Effect.gen(function*() { ... }) }` directly, since createEffectApp already wires the resolver. Import Effect from "effect" and TodoService/TodoServiceR from "../services/TodoService.ts".
    - `islands/Counter.tsx`: Simple interactive island — `import { useState } from "preact/hooks"`, default export `Counter()` component with a count state, increment button. Identical across all three apps. Purpose: ensures all apps have at least one island for bundle size comparison.

    **freak-plain-app (plain handler, port 8002):**
    - `deno.json`: Same as freak-app but WITHOUT `@fresh/effect` or `effect` imports. Only `@fresh/core`, `@fresh/core/dev`, `@fresh/core/runtime`, `preact`, `preact/hooks`. Same compilerOptions.
    - `main.ts`: `import { App, staticFiles } from "@fresh/core"`, `export const app = new App().use(staticFiles()).fsRoutes()`. No Effect, no createEffectApp.
    - `dev.ts`: Identical to freak-app's dev.ts.
    - `routes/index.tsx`: Identical to freak-app (renders heading with "Freak Benchmark (Plain)" text, includes Counter island).
    - `routes/api/todos.ts`: Plain sync handler — `export const handler = { GET: (_ctx: FreshContext) => { const todos = [{id: "1", text: "Benchmark todo", done: false}]; return new Response(JSON.stringify(todos), { headers: {"content-type": "application/json"} }); } }`. Use `import type { FreshContext } from "@fresh/core"` for the handler parameter type.
    - `islands/Counter.tsx`: Identical to freak-app.

    **upstream-app (upstream Fresh, port 8003):**
    - `deno.json`: imports `@fresh/core` from `jsr:@fresh/core@2.2.0`, `@fresh/core/dev` from `jsr:@fresh/core@2.2.0/dev`, `@fresh/core/runtime` from `jsr:@fresh/core@2.2.0/runtime`, `preact` from `npm:preact@^10.28.3`, `preact/hooks` from `npm:preact@^10.28.3/hooks`. Same compilerOptions and tasks.
    - `main.ts`: `import { App, staticFiles } from "@fresh/core"`, `export const app = new App().use(staticFiles()).fsRoutes()`. Identical to freak-plain-app.
    - `dev.ts`: Identical to freak-app's dev.ts.
    - `routes/index.tsx`: Same structure, heading says "Fresh Benchmark (Upstream)".
    - `routes/api/todos.ts`: Same plain handler as freak-plain-app. Use `import type { FreshContext } from "@fresh/core"` for the handler parameter type.
    - `islands/Counter.tsx`: Identical to freak-app.

    **IMPORTANT patterns to follow:**
    - Do NOT include tailwindcss in any benchmark app — it adds build complexity without measuring framework overhead.
    - The `_app.tsx` layout is optional for benchmark apps — skip it to keep things minimal. If the builder requires it, add a minimal one.
    - `fsRoutes()` is called WITHOUT arguments — it auto-discovers `routes/` and `islands/` relative to the module.
    - `staticFiles()` middleware is included in all three apps for parity.
    - All three Counter islands must be byte-for-byte identical.
    - The freak-app todos handler returns EXACTLY the same JSON as the plain/upstream handlers for fair throughput comparison.
  </action>
  <verify>
    1. `ls packages/benchmarks/apps/freak-app/routes/api/todos.ts` — file exists
    2. `ls packages/benchmarks/apps/freak-plain-app/routes/api/todos.ts` — file exists
    3. `ls packages/benchmarks/apps/upstream-app/routes/api/todos.ts` — file exists
    4. `diff packages/benchmarks/apps/freak-app/islands/Counter.tsx packages/benchmarks/apps/freak-plain-app/islands/Counter.tsx` — identical
    5. `diff packages/benchmarks/apps/freak-plain-app/islands/Counter.tsx packages/benchmarks/apps/upstream-app/islands/Counter.tsx` — identical
    6. `grep "createEffectApp" packages/benchmarks/apps/freak-app/main.ts` — found
    7. `grep "Effect.gen" packages/benchmarks/apps/freak-app/routes/api/todos.ts` — found
    8. `grep "jsr:@fresh/core@2.2.0" packages/benchmarks/apps/upstream-app/deno.json` — found
  </verify>
  <done>
    Three benchmark apps exist at packages/benchmarks/apps/{freak-app,freak-plain-app,upstream-app}/ with identical route structures (GET /, GET /api/todos, Counter island), differing only in how the handler is wired (Effect vs plain vs upstream).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add benchmarks to workspace and verify all apps build</name>
  <files>
    deno.json
  </files>
  <action>
    1. Read the root `deno.json` and add `"./packages/benchmarks"` to the `workspace` array. Keep existing entries. The benchmarks package is NOT a publishable package so it won't interfere with `deno publish`.

    2. Run `deno cache` on each app's main.ts to warm the module cache (downloads any JSR/npm deps):
       - `deno cache packages/benchmarks/apps/freak-app/main.ts`
       - `deno cache packages/benchmarks/apps/freak-plain-app/main.ts`
       - `deno cache packages/benchmarks/apps/upstream-app/main.ts`

    3. Build each app using the dev.ts build command:
       - `deno run -A packages/benchmarks/apps/freak-app/dev.ts build`
       - `deno run -A packages/benchmarks/apps/freak-plain-app/dev.ts build`
       - `deno run -A packages/benchmarks/apps/upstream-app/dev.ts build`

    If any build fails, debug and fix the app code. Common issues:
    - Missing imports in deno.json (add them)
    - `fsRoutes()` needing a different call pattern (check if it needs the import.meta.url)
    - `@fresh/core/dev` import path differences between local and JSR
    - Builder needs to find routes/ relative to main.ts — ensure dev.ts is in the same dir as main.ts

    4. After successful builds, clean up the _fresh directories (they are gitignored):
       `rm -rf packages/benchmarks/apps/freak-app/_fresh packages/benchmarks/apps/freak-plain-app/_fresh packages/benchmarks/apps/upstream-app/_fresh`
  </action>
  <verify>
    1. `grep "packages/benchmarks" deno.json` — workspace member registered
    2. Build each app and confirm exit code 0:
       - `deno run -A packages/benchmarks/apps/freak-app/dev.ts build && echo "freak-app OK"`
       - `deno run -A packages/benchmarks/apps/freak-plain-app/dev.ts build && echo "freak-plain-app OK"`
       - `deno run -A packages/benchmarks/apps/upstream-app/dev.ts build && echo "upstream-app OK"`
    3. `ls packages/benchmarks/apps/freak-app/_fresh/server.js` — build output exists
  </verify>
  <done>
    All three benchmark apps build without errors. `packages/benchmarks` is registered as a workspace member in root deno.json. Build artifacts (_fresh/) are gitignored.
  </done>
</task>

</tasks>

<verification>
- Three apps exist with identical route structure (index.tsx, api/todos.ts, Counter.tsx)
- Counter.tsx is identical across all three apps
- freak-app uses createEffectApp + Effect.gen handler
- freak-plain-app uses plain App + sync/async handler
- upstream-app uses jsr:@fresh/core@2.2.0 + sync/async handler
- All three apps build successfully with `deno run -A dev.ts build`
- packages/benchmarks is in root workspace
- _fresh/ build artifacts are gitignored
</verification>

<success_criteria>
1. `deno run -A packages/benchmarks/apps/freak-app/dev.ts build` exits 0
2. `deno run -A packages/benchmarks/apps/freak-plain-app/dev.ts build` exits 0
3. `deno run -A packages/benchmarks/apps/upstream-app/dev.ts build` exits 0
4. `grep "createEffectApp" packages/benchmarks/apps/freak-app/main.ts` matches
5. `grep "jsr:@fresh/core@2.2.0" packages/benchmarks/apps/upstream-app/deno.json` matches
6. `diff` of Counter.tsx across all three apps shows no differences
</success_criteria>

<output>
After completion, create `.planning/phases/13-benchmarks-freak-vs-fresh/13-01-SUMMARY.md`
</output>
