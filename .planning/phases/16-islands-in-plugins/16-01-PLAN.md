---
phase: 16-islands-in-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/fresh/tests/plugin_islands_test.tsx
autonomous: true

must_haves:
  truths:
    - "A plugin that registers an island via app.islands() has that island appear in the host's BuildCache after mountApp() + setBuildCache()"
    - "SSR HTML from a route using a plugin-registered island contains frsh:island markers with the component's export name"
    - "Two plugins mounting islands with the same export name on the same host produce unique island names (no collision)"
  artifacts:
    - path: "packages/fresh/tests/plugin_islands_test.tsx"
      provides: "ISLD-01, ISLD-02, ISLD-03 requirement tests"
      min_lines: 80
  key_links:
    - from: "plugin_islands_test.tsx"
      to: "packages/fresh/src/app.ts"
      via: "createPlugin + mountApp + islands()"
      pattern: "createPlugin|mountApp|islands"
    - from: "plugin_islands_test.tsx"
      to: "packages/fresh/src/build_cache.ts"
      via: "setBuildCache triggers IslandPreparer"
      pattern: "setBuildCache|islandRegistry"
    - from: "plugin_islands_test.tsx"
      to: "packages/fresh/src/runtime/server/preact_hooks.ts"
      via: "SSR render produces frsh:island markers"
      pattern: "frsh:island"
---

<objective>
Write the three ISLD-0x requirement tests that formally verify plugin islands work
end-to-end: a plugin calling app.islands(), SSR producing island markers, and two
plugins with distinct islands producing no name collisions.

Purpose: Phase 16 validates that the already-implemented BuildCache island aggregation
path works correctly through the Plugin<Config,S,R> interface from Phase 15. Research
confirmed no production source changes are needed -- the feature is already implemented
in app.ts (mountApp island merge, lines 421-423) and build_cache.ts (IslandPreparer).
This plan writes the formal requirement tests.

Output: `packages/fresh/tests/plugin_islands_test.tsx` with 4-5 tests covering
ISLD-01 (BuildCache aggregation), ISLD-02 (SSR marker generation), and ISLD-03
(two-plugin chunk name collision prevention).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-islands-in-plugins/16-RESEARCH.md

# Key source files (read-only reference -- no changes needed)
@packages/fresh/src/app.ts (lines 187-202: setBuildCache island registration; lines 389-433: mountApp with Plugin overload + island merge)
@packages/fresh/src/build_cache.ts (lines 92-118: IslandPreparer with UniqueNamer)
@packages/fresh/src/plugin.ts (Plugin<Config,S,R> interface + createPlugin factory)
@packages/fresh/src/mod.ts (public exports including createPlugin, Plugin, FreshScripts)

# Test patterns to follow
@packages/fresh/tests/islands_test.ts (MockBuildCache unit tests for islands + mountApp)
@packages/fresh/tests/islands_ssr_demo_test.tsx (SSR HTML marker tests using ctx.render)
@packages/fresh/tests/plugin_test.ts (createPlugin + mountApp integration tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin_islands_test.tsx with ISLD-01, ISLD-02, ISLD-03 tests</name>
  <files>packages/fresh/tests/plugin_islands_test.tsx</files>
  <action>
Create `packages/fresh/tests/plugin_islands_test.tsx` with JSX support (`/** @jsxImportSource preact */`).

Follow the established patterns from `islands_test.ts` (unit-level MockBuildCache tests)
and `islands_ssr_demo_test.tsx` (SSR HTML marker tests using ctx.render).

Import these utilities:
- `App`, `createPlugin` from `@fresh/core`
- `setBuildCache` from `../src/internals.ts`
- `MockBuildCache` from `../src/test_utils.ts`
- `expect` from `@std/expect`
- `ComponentType` type from `preact`

Define simple fixture island components as plain functions at the top of the file:
- `function CounterIsland({ count }: { count: number }) { return <p class="count">{count}</p>; }`
- `function GreetIsland({ name }: { name: string }) { return <p class="greet">Hello {name}</p>; }`
- `function CounterIsland2({ count }: { count: number }) { return <p class="count2">{count}</p>; }`

Create helper functions matching the patterns in existing tests:
- `makeApp()` — creates App + MockBuildCache + setBuildCache, returns both
- `renderPage(app, path)` — calls `app.handler()`, fetches path, returns HTML text

Write these tests:

**Test 1 — ISLD-01: Plugin island appears in host BuildCache after mountApp**
```
Deno.test("ISLD-01: plugin island registered via createPlugin + mountApp appears in host BuildCache", () => {
  const plugin = createPlugin({ label: "counter" }, (_config) => {
    const app = new App();
    app.islands({ CounterIsland }, "counter-island-chunk");
    return app;
  });

  const host = new App();
  host.mountApp("/plugin", plugin);

  const cache = new MockBuildCache([], "production");
  setBuildCache(host, cache, "production");

  expect(cache.islandRegistry.has(CounterIsland as ComponentType)).toBe(true);
  const entry = cache.islandRegistry.get(CounterIsland as ComponentType);
  expect(entry).toBeDefined();
  expect(entry!.file).toBe("counter-island-chunk");
  expect(entry!.exportName).toBe("CounterIsland");
});
```

**Test 2 — ISLD-01 (multiple): host + plugin islands both appear in registry**
```
Deno.test("ISLD-01: host islands and plugin islands both appear in merged registry", () => {
  const plugin = createPlugin({}, (_config) => {
    const app = new App();
    app.islands({ CounterIsland }, "plugin-counter");
    return app;
  });

  const host = new App();
  host.islands({ GreetIsland }, "host-greet");
  host.mountApp("/p", plugin);

  const cache = new MockBuildCache([], "production");
  setBuildCache(host, cache, "production");

  expect(cache.islandRegistry.size).toBe(2);
  expect(cache.islandRegistry.has(CounterIsland as ComponentType)).toBe(true);
  expect(cache.islandRegistry.has(GreetIsland as ComponentType)).toBe(true);
});
```

**Test 3 — ISLD-02: SSR HTML from plugin island route contains frsh:island markers**

CRITICAL: The SSR marker uses the **component export name** (e.g., `CounterIsland`),
NOT the chunk name (e.g., `counter-island`). The success criterion in the roadmap
says `counter-island` but research confirmed the actual marker format uses the
PascalCase component name. Assert `frsh:island` and `CounterIsland` in the HTML.

```
Deno.test("ISLD-02: SSR HTML from plugin route contains frsh:island markers", async () => {
  const plugin = createPlugin({}, (_config) => {
    const app = new App();
    app.islands({ CounterIsland }, "counter-island-chunk");
    app.get("/widget", (ctx) =>
      ctx.render(
        <html>
          <body>
            <CounterIsland count={7} />
          </body>
        </html>,
      )
    );
    return app;
  });

  const host = new App();
  host.mountApp("/p", plugin);

  const cache = new MockBuildCache([], "production");
  setBuildCache(host, cache, "production");

  const handler = host.handler();
  const res = await handler(new Request("http://localhost/p/widget"));
  const html = await res.text();

  // SSR content present
  expect(html).toContain("7");
  // Island hydration markers present (uses export name, not chunk name)
  expect(html).toContain("frsh:island");
  expect(html).toContain("CounterIsland");
});
```

**Test 4 — ISLD-03: Two plugins with same-named export get unique island names**
```
Deno.test("ISLD-03: two plugins with same-named component export get unique island names (no collision)", () => {
  // Both plugins export a component named "CounterIsland" from different chunks
  const pluginA = createPlugin({ id: "a" }, (_config) => {
    const app = new App();
    app.islands({ CounterIsland }, "chunk-a");
    return app;
  });

  const pluginB = createPlugin({ id: "b" }, (_config) => {
    const app = new App();
    app.islands({ CounterIsland: CounterIsland2 }, "chunk-b");
    return app;
  });

  const host = new App();
  host.mountApp("/a", pluginA);
  host.mountApp("/b", pluginB);

  const cache = new MockBuildCache([], "production");
  setBuildCache(host, cache, "production");

  // Both islands registered
  expect(cache.islandRegistry.has(CounterIsland as ComponentType)).toBe(true);
  expect(cache.islandRegistry.has(CounterIsland2 as ComponentType)).toBe(true);

  // Collect island names from registry
  const names = new Set<string>();
  for (const [, island] of cache.islandRegistry) {
    names.add(island.name);
  }

  // Names must be unique (UniqueNamer appends _1 suffix for collisions)
  expect(names.size).toBe(2);
});
```

**Test 5 — ISLD-03: Two plugins with distinct islands on same host — no collision in SSR**
```
Deno.test("ISLD-03: two plugins with distinct islands both produce SSR markers", async () => {
  const pluginA = createPlugin({}, (_config) => {
    const app = new App();
    app.islands({ CounterIsland }, "chunk-counter");
    app.get("/view", (ctx) =>
      ctx.render(
        <html><body><CounterIsland count={10} /></body></html>,
      )
    );
    return app;
  });

  const pluginB = createPlugin({}, (_config) => {
    const app = new App();
    app.islands({ GreetIsland }, "chunk-greet");
    app.get("/view", (ctx) =>
      ctx.render(
        <html><body><GreetIsland name="world" /></body></html>,
      )
    );
    return app;
  });

  const host = new App();
  host.mountApp("/a", pluginA);
  host.mountApp("/b", pluginB);

  const cache = new MockBuildCache([], "production");
  setBuildCache(host, cache, "production");

  const handler = host.handler();

  // Plugin A route
  const resA = await handler(new Request("http://localhost/a/view"));
  const htmlA = await resA.text();
  expect(htmlA).toContain("frsh:island");
  expect(htmlA).toContain("CounterIsland");
  expect(htmlA).toContain("10");

  // Plugin B route
  const resB = await handler(new Request("http://localhost/b/view"));
  const htmlB = await resB.text();
  expect(htmlB).toContain("frsh:island");
  expect(htmlB).toContain("GreetIsland");
  expect(htmlB).toContain("world");
});
```

Note about ISLD-02 SC-2 (client hydration): Full client hydration verification requires
a real island file on disk + `buildProd` + `withBrowserApp` flow, which requires the
build pipeline to know about plugin island specifiers. This is beyond the scope of
unit testing. Add a comment in the test file noting that full browser hydration should
be verified manually via `deno task dev` in the example app (Phase 17 will cover this).
  </action>
  <verify>
Run: `cd packages/fresh && deno test --allow-env --allow-read --allow-net tests/plugin_islands_test.tsx`

All 5 tests must pass:
- ISLD-01: plugin island appears in BuildCache (registry check)
- ISLD-01: host + plugin islands both in merged registry
- ISLD-02: SSR markers contain `frsh:island` and `CounterIsland`
- ISLD-03: two plugins same-named export get unique names
- ISLD-03: two plugins distinct islands both produce SSR markers
  </verify>
  <done>
All 5 tests pass. `plugin_islands_test.tsx` exists with formal requirement coverage for
ISLD-01 (BuildCache aggregation via createPlugin + mountApp), ISLD-02 (SSR island markers
in rendered HTML using component export name), and ISLD-03 (UniqueNamer collision prevention
with two plugins + multi-plugin SSR marker verification).
  </done>
</task>

</tasks>

<verification>
1. `cd packages/fresh && deno test --allow-env --allow-read --allow-net tests/plugin_islands_test.tsx` — all 5 tests pass
2. `cd packages/fresh && deno test --allow-env --allow-read --allow-net tests/plugin_test.ts` — existing plugin tests still pass (no regression)
3. `cd packages/fresh && deno test --allow-env --allow-read --allow-net tests/islands_test.ts` — existing island unit tests still pass
4. `cd packages/fresh && deno test --allow-env --allow-read --allow-net tests/islands_ssr_demo_test.tsx` — existing SSR demo tests still pass
</verification>

<success_criteria>
- 5 new tests in `plugin_islands_test.tsx` all pass
- ISLD-01 verified: plugin islands appear in host BuildCache after createPlugin + mountApp + setBuildCache
- ISLD-02 verified: SSR HTML contains `frsh:island` markers with component export name `CounterIsland` (not chunk name)
- ISLD-03 verified: two plugins with same-named exports get unique island names; both produce correct SSR markers
- No existing tests broken
- No production source code changes required (all testing the already-implemented feature)
</success_criteria>

<output>
After completion, create `.planning/phases/16-islands-in-plugins/16-01-SUMMARY.md`
</output>
