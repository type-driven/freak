import Counter from "../islands/Counter.tsx";

export default function IndexPage() {
  return (
    <html>
      <head>
        <title>Fresh Benchmark (Upstream)</title>
      </head>
      <body>
        <h1>Fresh Benchmark (Upstream)</h1>
        <Counter />
      </body>
    </html>
  );
}
