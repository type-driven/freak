/**
 * @module
 * Preact atom hooks for Effect v4 reactivity.
 *
 * Re-exports from @fresh/effect/island for backward compatibility.
 * Prefer importing from @fresh/effect/island directly.
 */

export {
  _checkOrphanedKeys,
  initAtomHydration,
  useAtom,
  useAtomSet,
  useAtomValue,
} from "@fresh/effect/island";
